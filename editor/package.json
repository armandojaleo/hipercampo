{
  "name": "hipercampo-viewer",
  "displayName": "%ext.displayName%",
  "description": "%ext.description%",
  "version": "0.9.15",
  "preview": true,
  "publisher": "armandojaleo",
  "license": "MIT",
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#080d3a",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/armandojaleo/hipercampo"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "keywords": [
    "hipercampo",
    "memory",
    "mcp",
    "claude",
    "codex",
    "ai-agents"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "hipercampo.showMemories",
        "title": "%cmd.showMemories%",
        "icon": "$(database)"
      },
      {
        "command": "hipercampo.refresh",
        "title": "%cmd.refresh%",
        "icon": "$(refresh)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "hipercampoView",
          "title": "Hipercampo",
          "icon": "media/brain.svg"
        }
      ]
    },
    "views": {
      "hipercampoView": [
        {
          "id": "hipercampo.home",
          "name": "%view.home%",
          "type": "webview"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "hipercampo.showMemories",
          "when": "view == hipercampo.home",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Hipercampo",
      "properties": {
        "hipercampo.command": {
          "type": "string",
          "default": "hipercampo",
          "description": "%config.command%"
        },
        "hipercampo.dbPath": {
          "type": "string",
          "default": "",
          "description": "%config.dbPath%"
        },
        "hipercampo.namespace": {
          "type": "string",
          "default": "",
          "description": "%config.namespace%"
        },
        "hipercampo.allNamespaces": {
          "type": "boolean",
          "default": true,
          "description": "%config.allNamespaces%"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "test": "npm run compile && node tests/i18n.test.js",
    "preview": "node tools/preview.js --all",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
